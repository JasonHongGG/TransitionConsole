# Frontend
VITE_AGENT_MODE=copilot
VITE_AGENT_API_BASE=http://localhost:7070

# Copilot agent server
PORT=7070
GITHUB_TOKEN=your_github_token_here
COPILOT_MODEL=gpt-5.2
COPILOT_CLI_PATH=
COPILOT_CLI_URL=
COPILOT_LOG_LEVEL=info
COPILOT_SYSTEM_PROMPT=
COPILOT_BOOT_PROMPT=Start testing the UI using the diagram data. Output JSON objects when reporting state transitions.
COPILOT_STEP_PROMPT=Continue with the next meaningful test step. Output JSON when possible.
